CREATE TABLE IF NOT EXISTS drug(
  drug_name         varchar(200) NOT NULL,
  generic_name        varchar(200) NOT NULL,
  brand_name    varchar(200) NOT NULL,
  dosage_form   varchar(200) NOT NULL,
  pharm_class       varchar(200)  NOT NULL
)